const ProBadge = () => {

  return (
    <span className="ml-2 bg-purple-100 text-purple-800 text-xs px-2 py-0.5 rounded-full">
      PRO
    </span>
  );
};

const FreeBadge = () => {

  return (
    <span className="ml-2 text-xs bg-blue-100 text-blue-800 px-1.5 py-0.5 rounded">
      FREE
    </span>
  );
};

export {ProBadge, FreeBadge};